<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.redtourism.mapper.NewsMapper">
    <select id="selectAll" resultType="News">
        SELECT * FROM news ORDER BY publish_time DESC
    </select>

    <select id="selectById" resultType="News">
        SELECT * FROM news WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="News" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO news (title, cover_image, source,content, article,author, publish_time, view_count)
        VALUES (#{title}, #{coverImage},#{source}, #{content}, #{article},#{author}, #{publishTime}, #{viewCount})
    </insert>

    <update id="update" parameterType="News">
        UPDATE news
        SET
        title = #{title},
        source = #{source},
        content = #{content},
        article = #{article},
        author = #{author},
        publish_time = #{publishTime},
        <if test="coverImage != null">
            cover_image = #{coverImage},
        </if>
        view_count = #{viewCount}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM news WHERE id = #{id}
    </delete>

    <update id="increaseViewCount">
        UPDATE news SET view_count = view_count + 1 WHERE id = #{newsId}
    </update>

    <select id="selectTopNews" resultType="News">
        SELECT * FROM news ORDER BY view_count DESC LIMIT #{limit}
    </select>

    <update id="updateCoverImage">
        UPDATE news SET cover_image = #{coverImage} WHERE id = #{id}
    </update>

    <select id="getViewCount" resultType="int">
        SELECT view_count FROM news where id = #{newsId}
    </select>

</mapper>