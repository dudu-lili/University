<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.redtourism.mapper.VideoMapper">

    <select id="selectAll" resultType="Video">
        SELECT *,
               IFNULL((SELECT COUNT(*) FROM `like` WHERE video_id = video.id), 0) AS like_count
        FROM video
    </select>


    <select id="selectById" resultType="Video">
        SELECT * FROM video WHERE id = #{id}
    </select>

    <!-- 确保insert语句正确映射到数据库 -->
    <insert id="insert" parameterType="Video" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO video(title, description, cover_image, video_url, view_count,
                  duration, create_time, category)
        VALUES (#{title}, #{description}, #{coverImage}, #{videoUrl}, #{viewCount},
        #{duration}, #{createTime}, #{category})
    </insert>

    <update id="update" parameterType="Video">
        UPDATE video
        SET title = #{title},
            description = #{description},
            cover_image = #{coverImage},
            video_url = #{videoUrl},
            duration = #{duration},
            category = #{category}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM video WHERE id = #{id}
    </delete>

    <select id="selectByCategory" resultType="Video">
        SELECT * FROM video
        WHERE category = #{category}
        ORDER BY create_time DESC
    </select>

    <select id="searchVideos" resultType="Video">
        SELECT * FROM video
        WHERE title LIKE CONCAT('%', #{keyword}, '%')
           OR description LIKE CONCAT('%', #{keyword}, '%')
    </select>

    <update id="increaseLikeCount">
        UPDATE video SET like_count = like_count + 1 WHERE id = #{id}
    </update>

    <update id="decreaseLikeCount">
        UPDATE video SET like_count = like_count - 1 WHERE id = #{id}
    </update>

    <select id="selectAllCategories" resultType="String">
        SELECT DISTINCT category FROM video
    </select>

    <select id="listTopVideos" resultType="Video">
        SELECT * FROM video
        ORDER BY like_count DESC
            LIMIT #{limit}
    </select>

    <select id="listFavoritesByUserId" resultType="Video">
        SELECT v.* FROM video v
                            JOIN favorite f ON v.id = f.video_id
        WHERE f.user_id = #{userId}
        ORDER BY f.create_time DESC
    </select>

    <select id="findAllOrderByViews" resultType="Video">
        SELECT * FROM video ORDER BY view_count ${order}
    </select>

    <select id="findAllOrderByLikes" resultType="Video">
        SELECT * FROM video ORDER BY like_count ${order}
    </select>

    <select id="findAllOrderByCreateTime" resultType="Video">
        SELECT * FROM video ORDER BY create_time ${order}
    </select>

    <delete id="batchDelete">
        DELETE FROM video WHERE id IN
        <foreach collection="videoIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <update id="increaseViewCount">
        UPDATE video SET view_count = view_count + 1 WHERE id = #{id}
    </update>
    <!-- 检查用户是否已点赞视频 -->
    <select id="checkIfUserLikedVideo" resultType="int">
        SELECT COUNT(*) FROM `like`
        WHERE user_id = #{userId} AND video_id = #{videoId}
    </select>

    <!-- 记录用户点赞行为 -->
    <insert id="recordUserLikeVideo">
        INSERT INTO `like`(user_id, video_id)
        VALUES (#{userId}, #{videoId})
    </insert>

    <!-- 取消用户点赞行为 -->
    <delete id="cancelUserLikeVideo">
        DELETE FROM `like`
        WHERE user_id = #{userId} AND video_id = #{videoId}
    </delete>

    <!-- 获取视频点赞数 -->
    <select id="getLikeCount" resultType="int">
        SELECT like_count FROM video WHERE id = #{id}
    </select>

    <select id="selectSameCategoryVideosExcludeSelf" resultType="Video">
        SELECT * FROM video
        WHERE category = #{category}
          AND id != #{excludeId}
        ORDER BY RAND()
            LIMIT 3
    </select>

</mapper>